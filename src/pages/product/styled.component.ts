import { Box, Card, styled } from "@mui/material";

export const CustomCard = styled(Card)({
    padding: '1rem',
    marginTop: '2.5rem'
});

export const FlexBoxSpaced = styled(Box)({
    display: 'flex',
    justifyContent: 'space-between'
});